node_modules/
out/
*.log
.DS_Store

# Never commit local keys or environment. Demo keys are generated per run under out/.
.env
.env.*
*.pem
*.key
cosign.key
cosign.pub

# Local Claude Code settings (machine paths, hook install)
.claude/settings.local.json
