title = "colophon"
[extend]
useDefault = true
[allowlist]
description = "Demo and test signing keys are generated at run time under out/ and never committed; synthetic fixture tokens use the ghp_ prefix with a documented FAKE marker."
paths = [
  '''^out/''',
  '''^node_modules/''',
  '''^packages/fixtures/scenarios/evidence-reader\.yaml$'''
]
